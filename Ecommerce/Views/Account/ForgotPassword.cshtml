@model PasswordDto
@{
}

<style>
    html, body {
        overflow-x: hidden;
    }

    ::selection {
        color: white;
        background-color: black;
    }

    body {
        height: 100%;
        margin: 0;
        justify-content: center;
        align-items: center;
        background-color: #f9f9f9;
    }

    .profile-container {
        max-width: 600px;
        margin-top: 270px;
        margin-bottom: 410px;
        padding: 80px;
        background-color: white;
        border-radius: 15px;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
    }

    .btn-container {
        display: flex;
        gap: 10px;
        justify-content: center;
    }

    .btn-submit {
        background-color: white;
        color: black;
        padding: 12px 20px;
        border: none;
        flex: 1;
        text-align: center;
        font-size: 16px;
        border-radius: 25px;
        transition: background-color 0.3s ease, transform 0.2s;
    }

    .btn-submit:hover {
        background-color: black;
        color: white;
        transform: translateY(-2px);
    }

    a.btn-submit {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        text-decoration: none;
    }

    input.form-control {
        border: 1px solid #ccc;
        border-radius: 8px;
        transition: border-color 0.3s ease, border-radius 0.3s ease;
    }

    input.form-control:focus {
        outline: none;
        border-color: black;
        box-shadow: none;
    }

    .form-floating {
        position: relative;
    }

    .form-floating label {
        padding-left: 10px;
        color: #666;
        transition: all 0.3s ease;
        position: absolute;
    }

    .form-control:not(:placeholder-shown) + label,
    .form-control:focus + label {
        top: -1px;
        left: 2px;
        font-size: 0.9rem;
        color: black;
    }

</style>

<div class="container profile-container">
    <h2 class="text-center mb-3">Reset your password</h2>
    <p class="text-center mb-4">We will send you an email to reset your password.</p>

    <form method="post">
        <div class="form-floating mb-3">
            <input class="form-control" name="email" value="@ViewBag.Email" placeholder=" " required/>
            <label class="form-label">Email</label>
            <span class="text-danger">@ViewBag.ErrorMessage</span>
        </div>

        @if (ViewBag.SuccessMessage != null)
        {
            <div class="alert alert-success alert-dismissible fade show" role="alert">
                <strong>@ViewBag.SuccessMessage</strong>
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        }

        <div class="btn-container mt-4">
            <button type="submit" class="btn-submit border border-dark w-100">Reset Password</button>
            <a class="btn-submit border border-dark w-100" asp-controller="Account" asp-action="Login">Cancel</a>
        </div>
    </form>
</div>
